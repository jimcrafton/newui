Ideas 





Create a python tool that can take external file based resources, like a png file, and convert into a window’s appropriate app resource, like a cursor or ICO, and then wrap the resource compiler (rc) and compile them 


Add docking?
Add tabs



How to handle running idle tasks outside main winproc and runloop, in hosted environment 



How to best attach the root view to an existing hwnd that is not a framework owned hwnd 



Be able to run without application class or frame class


Fixes:

Scrolling



How views are painted and how to properly paint only the necessary views

How to best attach the root view to an existing hwnd that is not a framework owned hwnd 



Be able to run without application class or frame class


Shapes

Support hit testing, given a point

Have a selection class? Support rectangular, circular, straight points, or points converted to curves


Selection hold ctrl +shift should allow for non contiguous characters to be selected 

Column mode alt + click

view needs potentially 2 rects - 
1) a client bounds that represents the visible rect it can draw in
2) a content bounds that represents the full size of it's content. 

content bounds may be the same as client. Layout processing ONLY affects the client bounds

At the moment we don't have support for different text styles
  ? so we've got to fix that too
  
  
Clipboard
Drag n Drop (within the framework)
Drag n Drop (external, needs COM support)
